name: Deployment

on:
  push:
    branches: [hexo] # only push events on source branch trigger deployment

jobs:
  hexo-deployment:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai

    steps:
    - name: Checkout source
      uses: actions/checkout@v2
      with:
        submodules: false

    - name: Setup Node.js
      uses: actions/setup-node@v1
      with:
        node-version: '12.x'

    - name: Install dependencies & Generate static files
      run: |
        node -v
        npm i -g hexo-cli
        npm i
        sed -i 's/{{/{$/g' node_modules/nunjucks/src/lexer.js
        sed -i 's/}}/$}/g' node_modules/nunjucks/src/lexer.js
        hexo clean
        hexo g
    - name: Deploy to Github Pages
      env:
        GIT_NAME: weyung
        GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
        REPO: github.com/weyung/weyung.github.io
        GH_TOKEN: ${{ secrets.GH_TOKEN }}
      run: |
        git config --global user.name $GIT_NAME
        git config --global user.email $GIT_EMAIL
        cd ./public
        git init
        git add .
        git stash
        git remote add origin https://$GH_TOKEN@$REPO
        git fetch origin main:main
        git checkout main
        git stash pop
        git add .
        git commit -m "Site deployed by GitHub Actions"
        git push "https://$GH_TOKEN@$REPO" main:main